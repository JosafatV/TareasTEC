#include <stdlib.h>
#include <stdio.h>
#include <time.h>

char *itoc (int num) {
	char store[50]; 
	sprintf(store, "%d", num);
	char* result = &store[0];
	return result;
}


int main() {
	char msg[10] = 'Hello';
	
	for (int i = 0; i <5; i++) {
		char *
	}
	return 0;
}
